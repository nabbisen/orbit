//! orbok-bench entry point.
//! Build and run on a machine with sufficient RAM (release binary).
//!
//! Usage: orbok-bench [N_DOCS] [OUTPUT_DIR]
fn main() {
    eprintln!("orbok-bench: run 'cargo run -p orbok-bench --release' on a full build machine.");
    eprintln!("The harness code lives in orbok_bench_lib crate.");
}
